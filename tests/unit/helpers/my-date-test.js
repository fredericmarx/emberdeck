import { myDate } from '../../../helpers/my-date';
import { module, test } from 'qunit';

module('Unit | Helper | my date');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = myDate(42);
  assert.ok(result);
});
