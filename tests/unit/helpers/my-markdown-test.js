import { myMarkdown } from '../../../helpers/my-markdown';
import { module, test } from 'qunit';

module('Unit | Helper | my markdown');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = myMarkdown(42);
  assert.ok(result);
});
